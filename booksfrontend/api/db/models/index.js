const { Book } = require('./book.model');
const { Shelf } = require('./bookshelf.model');
const { Note } = require('./note.model');
const { User } = require('./user.model');

module.exports = {
    Book,
    Shelf,
    Note,
    User
}